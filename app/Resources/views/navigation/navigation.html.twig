<ul class="navigation">
    <li><a href="{{ path('about_me') }}">Über mich</a></li>
    <li class="dropdown">
        Lessons


        <div class="dropdown-content">
            {{ render(controller(
                'AppBundle:Navigation:getElements'
            )) }}
        </div>
    </li>

    {{ render(controller(
        'AppBundle:Navigation:setLoginState'
    )) }}
</ul>
