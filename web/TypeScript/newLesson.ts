/**
 * Created by yorrickschwappacher on 14.10.16.
 */

var addLatex = document.getElementById('addLatex');
addLatex.addEventListener('click', function () {
   addLatex.innerText = addLatex.innerText.toString() + "<Latex>";
});

var ytLink = document.getElementById('lesson_link');
ytLink.addEventListener('click()', function () {
   ytLink.textContent = "rofl";
   ytLink.innerText = "omfg";
    ytLink.hidden = true;
});